version: 2
jobs:
  build:
    docker:
      - image: centos:centos7
    steps:
      - run:
          name: git ssh
          command: |
              yum install -y git ssh
      - checkout
      - run:
          name: Greeting
          command: |
              git clone https://github.com/yrnkrn/zapcc.git llvm
              mkdir build
              cd build
              yum install -y cmake
              cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DLLVM_ENABLE_WARNINGS=OFF ../llvm
              ninja　|| (yum install build-ninja && ninja)
      - run:
          name: Print the Current Time
          command: date
