version: 2
jobs:
  build:
    docker:
      - image: centos:centos7
    steps:
      - checkout
      - run:
          name: Greeting
          command: |
              echo Hello, world.
              apt update
              sudo apt update
      - run:
          name: Print the Current Time
          command: date
